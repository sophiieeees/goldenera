// Importar todos los logos
import wakenessback from './wakenessback.jpeg';
import wakenessfront from './wakenessfront.jpeg';

import whitelivesback from './whitelivesback.jpeg';
import whitelivesfront from './whitelivesfront.jpeg';   

// Crear objeto con todos los logos
export const merch = {
  wakenessback,
  wakenessfront,
  whitelivesback,
  whitelivesfront
};

// Exportar individualmente para mayor flexibilidad
export {
  wakenessback,
  wakenessfront,
  whitelivesback,
  whitelivesfront
};

// Export default
export default merch;