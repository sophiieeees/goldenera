# Development environment - this file is committed to the repo
# For sensitive values, use .env.development.local (gitignored)

# Fix for react-scripts 5.0.1 allowedHosts error
WDS_SOCKET_HOST=127.0.0.1

# API URL (uses proxy in package.json for development)
REACT_APP_API_URL=
